# Required for encryption/signing of submission tokens
APP_SECRET="change-me-to-a-long-random-string"

# Optional: If provided, the site will generate & assess tasks using OpenAI.
OPENAI_API_KEY=""

# Optional: Model name (defaults in code if empty)
OPENAI_MODEL="gpt-4o-mini"

# Optional: If you want to hard-disable OpenAI even when a key exists
DEMO_MODE="false"
